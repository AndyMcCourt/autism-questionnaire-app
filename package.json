{
  "scripts": {
    "start": "python3 -m http.server 5173",
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir dist && cp index.html dist/ && cp -R src dist/src"
  },
  "dependencies": {},
  "devDependencies": {}
}
